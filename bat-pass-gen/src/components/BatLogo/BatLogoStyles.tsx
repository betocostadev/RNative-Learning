import { StyleSheet } from 'react-native'

export const BatLogoStyles = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 30,
    color: '#E5BF3C',
  },
})
